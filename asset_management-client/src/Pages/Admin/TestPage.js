import { Button} from "antd";
const TestPage = () => {
    return (
        <Button type="primary" >
        <a href={`/edit/SD2`}>View Detail</a>
        </Button>
    );
  };
  
  export default TestPage;